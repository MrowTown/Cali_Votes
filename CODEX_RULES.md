# Codex Rules (Do Not Violate)

- Work only on branch: sandbox (branched from dev).
- If you change a file, output the ENTIRE file (full replacement). No snippets.
- Admin must not use dynamic google.script.run[fnName]; use explicit calls.
- Auth must use 6-digit email codes (no magic links, no passwords).
- Do not break uploads (iOS JPEG + PNG).
- Do not remove origin allowlist entries for ifuckfans.com and www.ifuckfans.com.
